{
  "routes": [
    {
      "method": "GET",
      "path": "/change-password",
      "handler": "change-password.changePasswordGet",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/change-password",
      "handler": "change-password.changePasswordPost",
      "config": {
        "policies": []
      }
    }
  ]
}
