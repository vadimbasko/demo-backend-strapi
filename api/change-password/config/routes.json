{
  "routes": [
    {
      "method": "GET",
      "path": "/change-password",
      "handler": "change-password.resetGet",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/change-password",
      "handler": "change-password.resetPost",
      "config": {
        "policies": []
      }
    }
  ]
}
